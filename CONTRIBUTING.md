# 贡献指南

感谢你想让 GEOkit 变好。这个项目还很小（核心代码 5000 行出头），
**保持它被一个人读得懂**是比多加一个功能更重要的事。

## 开发

```bash
npm install
npm run dev        # http://localhost:3210
npm run typecheck  # 提交前必跑
npm run build      # 提交前必跑
```

## 三条硬规矩

### 1. 抓不到就是抓不到

任何接口在拿不到真实数据时，**必须**返回明确的失败状态（`blocked` / `unconfigured` / `error`）
并写明原因与解决方向。

❌ 不允许：估算排名、返回缓存旧数据冒充实时结果、用随机数填充。
✅ 正确示范：`src/lib/serp.ts` 里 `status: "blocked"` 的处理。

理由很实在：用户会拿这个数据做投放决策。一个漂亮的假数字比一句"这次没抓到"有害得多。

### 2. 不新增运行时依赖

核心引擎（HTML 解析、位次计算、robots 解析）全部自研，直接依赖目前只有 4 个。
想加库的 PR 需要在描述里说明：为什么自研不可行、这个库的维护状况、打包体积影响。

### 3. 引擎适配器要对着真实页面写

中文搜索引擎的 HTML 结构会变，而且各家把结果包成自家中转链接的做法不一样
（百度靠 `mu` 属性、360 靠 `data-mdurl`、搜狗靠 `citeLinkClass` 文本）。

改 `src/lib/serp.ts` 时请对着**真实返回页**核对，不要凭猜测写正则。
PR 里请附上你验证时用的关键词与抓到的样本域名。

## 提交

- 一个 PR 一件事
- `npm run typecheck && npm run build` 全绿
- 涉及引擎解析改动的，请贴一段验证输出

## 协议

MIT。
